# Use the official JMX Exporter image
FROM bitnami/jmx-exporter:latest

# This exporter runs as a java agent, and needs to be attached to a JVM.
# The Dockerfile is simple, but the usage is more complex.
# See the documentation for more details.

# Expose the default JMX Exporter port
EXPOSE 5556
